T4 — Parallel chunks on separate QUIC connections
=================================================

Category:
    core

Purpose:
    Best general multicore/RSS/hint-mapping test: several UDP flows may activate several RX queues and DPDK lcores simultaneously.

EXACT TEST TOPOLOGY AND COUNTS
------------------------------
Unique source files used:             4
Total file requests:                  40
Total QUIC connections:               40
Maximum concurrent QUIC connections:  4
Total QUIC streams:                   40
Maximum concurrent QUIC streams:      4
Total UDP 5-tuple flows:              40
Maximum concurrent UDP flows:         4

SERVER:
    MsQuic worker CPUs/lcores:        16,17,18,19
    Configured MsQuic workers:        4
    Configured MsQuic partitions:     4
    Distinct active owner partitions: 1–4 distinct active partitions possible
    Configured DPDK lcores:           8,9 (2 lcores)
    Active DPDK lcores during traffic: 1–2
    Dedicated TX owner lcore:         8

CLIENT:
    MsQuic worker CPUs/lcores:        20,21,22,23
    Configured MsQuic workers:        4
    Configured MsQuic partitions:     4
    Distinct active owner partitions: 1–4 distinct active partitions possible
    Configured DPDK lcores:           12,13 (2 lcores)
    Active DPDK lcores during traffic: 1–2
    Dedicated TX owner lcore:         12

Important:
    "Active owner partitions" is exact for zero/single-concurrent-connection
    tests. With parallel connections, each connection has one owner, but two
    connections may hash to the same partition; therefore the distinct active
    partition count is a bounded range and must be verified from logs.

Workload configuration:
    workload kind:            parallel_connections
    default GreenQUIC mode:   plus
    multicore RSS:            enabled
    gap:                      50000 us
    repetitions/batches:      10
    connections per batch:    4
    streams per connection:   1
    maximum configured sleep: 2 us

Requested server paths:
    /chunks/chunk_001.bin
    /chunks/chunk_002.bin
    /chunks/chunk_003.bin
    /chunks/chunk_004.bin

Object relationships:
Connection:
    Many active QUIC connections at the same time.

Stream:
    One stream per connection.

UDP/RSS flow:
    Many UDP 5-tuples. RSS can distribute them across queues.

DPDK lcore:
    Multiple DPDK RX lcores can be active. V18 uses one dedicated TX consumer to protect ordering.

MsQuic partition:
    Multiple MsQuic partitions can be active and their hints are mapped to configured DPDK lcores.

Important ownership correction:
    MsQuic decides which partition owns a connection.
    RSS/NIC decides which RX queue/lcore receives a UDP flow.
    GreenQUIC configuration maps partition -> DPDK lcore only for hints.
    This mapping does not force RSS to select the same lcore.

Mapping used by the generated default configuration:
SERVER:
    DPDK lcores: 8,9
    QUIC worker CPUs: 16,17,18,19

    P0 -> CPU 16 -> DPDK lcore 8
    P1 -> CPU 17 -> DPDK lcore 8
    P2 -> CPU 18 -> DPDK lcore 9
    P3 -> CPU 19 -> DPDK lcore 9

CLIENT:
    DPDK lcores: 12,13
    QUIC worker CPUs: 20,21,22,23

    P0 -> CPU 20 -> DPDK lcore 12
    P1 -> CPU 21 -> DPDK lcore 12
    P2 -> CPU 22 -> DPDK lcore 13
    P3 -> CPU 23 -> DPDK lcore 13

Diagram:
                              CLIENT                                      SERVER
                              |                                             |
                              |--- Conn 1, Stream 1: GET chunk_001 -------->|
                              |--- Conn 2, Stream 1: GET chunk_002 -------->|
                              |--- Conn 3, Stream 1: GET chunk_003 -------->|
                              |--- Conn 4, Stream 1: GET chunk_004 -------->|
                              |                                             |
                              |                         RSS / NIC            |
                              |                         flows -> RX queues   |
                              |                         queues -> lcores     |
                              |                                             |
                              |<---------- parallel data responses ---------|
                              |--- ACKs for active connections ------------>|
                              |                                             |
                              |=============== inter-batch gap ============|
                              | lcores cool independently                    |
                              | partitions become inactive                  |
                              |=============================================|

Signals expected:
    Per-lcore bursts/queue counts, partition-local ACK/CUBIC hints, shared TX-ring pressure on the TX owner, and independent empty-pull counters.

Energy management during ACTIVE periods:
    Each lcore reacts to its own RX pressure and partition-local hints. The TX owner also reacts to the shared TX ring. One lcore may max while another only keeps or steps up.

Energy management during IDLE periods:
    After a batch, lcores should freq_down/min/sleep independently when their own queues are empty. Global file-transfer hints must not blindly heat every lcore.

Expected energy result:
    Medium to high, especially when RSS is skewed or after long inter-batch gaps.

What to measure:
    * total package joules and average package power on both endpoints;
    * elapsed time, throughput and joules per GB/request;
    * per-lcore frequency residency and C-state residency;
    * RX/TX bursts, empty pulls, queue/ring pressure and EWMA pressure;
    * selected GreenQUIC action, total sleep requests and hint timeline;
    * QUIC RTT, ACK delay, retransmissions, recovery interval and completion time.

Per-role files in this testcase:
    server/dpdk.ini
    server/msquic_config.txt
    server/run.sh
    server/run_preflight.sh
    client/dpdk.ini
    client/msquic_config.txt
    client/run.sh
    client/run_preflight.sh
    TOPOLOGY.env

Run — server machine:
    ./server/run_preflight.sh
    ./server/run.sh plus

Run — client machine:
    SERVER_HOST=<server-ip> ./client/run_preflight.sh
    SERVER_HOST=<server-ip> ./client/run.sh plus

Compare modes:
    Use ./run_matrix.sh, or run the same workload with:
        ./server/run.sh off|basic|plus
        ./client/run.sh off|basic|plus

Outputs:
    logs/       complete server/client stdout and timestamps
    results/    generated dpdk.ini snapshots and RAPL/timing JSON
    runtime/    exact role-specific dpdk.ini used by the current run

Limitations and interpretation:
    The stock client creates random UDP source ports. It creates multiple RSS flows but cannot force a specific flow-to-queue mapping. Verify mapping from DPDK logs or use a custom client with fixed source ports for deterministic RSS tests.

IN-PROCESS CLIENT REQUIREMENT:
    A valid two-sided GreenQUIC energy run requires INPROCESS_CLIENT_BIN.
    It keeps one MsQuic/DPDK process alive across all connections/batches.
    --approximate launches stock client processes and is not a valid client-side
    empty-poll/DVFS measurement. For sequential tests it can still exercise the
    persistent server; for parallel tests it is only a server-load approximation.

Do not change useful work between OFF, BASIC and PLUS. Keep payload size, repetitions,
source-port strategy, CPU pinning, server temperature and network conditions constant.

Final V18 policy expectations
-----------------------------
* Each lcore updates only the physical signals for directions it owns.
* RX and TX remain separate until the final per-lcore max action.
* Queue/ring backlog should dominate when parallel work exceeds service capacity.
* Save and compare both dpdk.ini and powermng.ini hashes for every run.
